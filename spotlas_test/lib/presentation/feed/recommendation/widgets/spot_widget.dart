import 'package:flutter/material.dart';

class SpotWidget extends StatelessWidget {
  const SpotWidget({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}